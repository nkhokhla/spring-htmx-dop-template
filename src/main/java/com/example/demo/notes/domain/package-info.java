/**
 * Domain data for the notes feature: immutable records and sealed result types.
 * No framework dependencies allowed here (enforced by ArchitectureTest).
 */
@NullMarked
package com.example.demo.notes.domain;

import org.jspecify.annotations.NullMarked;
